<!-- style-guard:off 변경 안내 문서 -->
# 업그레이드 안내

새 판이 나오면 여기 맨 위에 적습니다. 이미 붙인 사이트는 **자기 판 이후 절만** 반영하면 됩니다.

받는 법(사이트 저장소 뿌리에서):

```bash
cd .claude/skills/clarity-connect && git pull
```

그다음 AI 코딩 도구에 이렇게 말합니다.

```
.claude/skills/clarity-connect/UPGRADE.md 읽고 우리 사이트에 아직 없는 판을 반영해.
관리자 화면이 바뀌는 건 시안 먼저 보여 주고 승인받아.
```

## 2026-09-22 판. 녹화를 읽고 제안까지

**대상**: 5단계(`docs/report-and-fix.md` 지시 A·B)를 넣은 사이트. 1~4단계만 한 사이트는 할 것이 없습니다.

**반영할 것**: `docs/report-and-fix.md` 의 **지시 C**. 참고 구현은 `scripts/clarity_recordings.py`.

**바뀌는 것**
1. 매일 보고의 주요 방문 표에 **재생 ↗** 링크가 붙습니다. 클라리티 녹화 목록을 받아 방문 기록과 짝짓습니다(실측 100건 중 87건).
2. 손님 화면 오류를 사이트가 직접 기록합니다. 클라리티는 오류가 난 방문을 걸러 주기만 하고 문구는 주지 않습니다.
3. 「고치기」가 "무엇을 바꿀지 모름"으로 멈추지 않습니다. 그 화면 녹화 30건과 오류를 읽고 제안을 쓴 뒤 바로 고쳐 미리보기까지 갑니다. 카드에 제안, 근거 녹화, 전후 캡처가 뜨고 [배포] 가 곧 승인입니다.
4. 첫 실전에서 잡힌 결함 두 가지를 막습니다. 원래 바뀌어 있던 파일을 수정 범위 위반으로 보던 것, 운영에도 원래 있는 화면 오류를 미리보기 실패로 보던 것.

**사람이 할 일**
- 새 열쇠는 없습니다. 보고에 쓰던 `CLARITY_API_TOKEN` 이 녹화 목록에도 통합니다.
- 재생 ↗ 링크는 클라리티에 로그인한 브라우저에서 열립니다.
- 화면 오류를 새로 기록하므로, 개인정보처리방침의 수집 항목에 "화면 오류 기록"이 있는지 봅니다.
- 관리자 화면 시안 승인, 배포 승인.

**비용**: 제안 한 번에 AI 호출 1번이 늘어납니다(코드 읽기만).

## 2026-09-15 판. 처음 공개

0~5단계 순서와 도구.
